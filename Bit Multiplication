def bit_multiplication(a, b):
    result = 0
    shift = 0
    while b > 0:
        if b & 1:
            result += a << shift
        b >>= 1
        shift += 1
    return result

print(bit_multiplication(5, 3))
